// Shared gameplay constants that several unrelated modules need to agree on
// (profile creation, floor/mode resets, save codes, the hearts HUD) — kept
// here once instead of a magic "3" (now 5) scattered across all of them.
export const DEFAULT_LIVES = 5
